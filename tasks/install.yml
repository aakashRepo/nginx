---
    - name: install wget
      apt:
        name: wget
        state: latest
        update_cache: yes  

    - name: Install {{package}}
      apt:
        name: "{{package}}"
        state: latest

